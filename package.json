{
  "name": "dshx-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "pnpm --filter dshx build && pnpm --filter create-dshx build",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build && pnpm --filter create-dshx typecheck && pnpm --filter create-dshx test && pnpm --filter create-dshx build",
    "phase-a:build": "pnpm run build && pnpm --filter @dshx/phase-a-fixture build",
    "test": "pnpm --filter dshx test",
    "typecheck": "pnpm --filter dshx typecheck",
    "smoke:rc2": "pnpm run build && node scripts/smoke-rc2.mjs"
  },
  "devDependencies": {
    "@types/node": "22.19.20",
    "typescript": "5.9.3",
    "vitest": "4.1.8"
  }
}
