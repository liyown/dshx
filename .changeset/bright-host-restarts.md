---
"@becomeopc/dshx": patch
---

Default successful Host rebuilds to automatic DSH restarts. Projects that need explicit control can set `dev.hostRestart` to `manual` and continue to restart with `r`.
