---
"@becomeopc/dshx": patch
"create-dshx": patch
---

Model DSH support by observable protocol generation, validate plugin peer ranges against the installed DSH version, and generate projects with independent public and local DSH versions.
